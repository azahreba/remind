document.addEventListener('DOMContentLoaded', function() {
  /*
  //storage
  chrome.storage.sync.set({ 'tasks': items });
  chrome.storage.sync.get('tasks', items => items);
  chrome.storage.sync.get(['projects', 'tasks'], items => items.tasks);

//notifications
  var opt = {
    type: 'basic',
    title: 'Title',
    message: 'Msg',
    iconUrl: 'images/rm_icon.png'
  };
  chrome.notifications.create('reset', opt, () => {});

  chrome.runtime.onInstalled.addListener(details => {
    console.log('previousVersion', details.previousVersion);
  });

  chrome.extension.sendMessage({
    type: "color-divs"
  });
  */
}, false);
